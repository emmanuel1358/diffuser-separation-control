"""Train and persist a weak but input-dependent naive model.

Generate ``model.manifest.json`` with schema_version, role=naive,
train/inference entrypoints, seed, and full SHA-256 artifact digests. The model
must earn a small positive score without approaching the reference.
"""

raise NotImplementedError("train the naive model and generate its model manifest")
