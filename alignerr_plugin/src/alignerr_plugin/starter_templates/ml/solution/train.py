"""Train and persist the committed reference model.

Write the model in a safe/native format and generate ``model.manifest.json``
with schema_version, role=reference, train/inference entrypoints, seed, and
full SHA-256 digests for every trained artifact.
"""

raise NotImplementedError("train the reference and generate its model manifest")
